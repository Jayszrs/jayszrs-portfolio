# Frontend

Folder ini berisi seluruh tampilan dan interaksi pengguna.

- `components/`: navbar, hero, section portfolio, modal, animasi, dan komponen admin pendukung.
- Route halaman tetap berada di `app/` karena Next.js App Router mewajibkan struktur tersebut.
- Import frontend menggunakan prefix `@/frontend/...`.
