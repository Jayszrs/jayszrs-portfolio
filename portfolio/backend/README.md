# Backend

Folder ini berisi logic dan data yang hanya digunakan server.

- `lib/content.js`: membaca dan menyimpan data portfolio.
- `data/content.json`: penyimpanan konten lokal.
- Endpoint HTTP berada di `app/api/` sesuai konvensi Next.js Route Handler.
- Import backend menggunakan prefix `@/backend/...`.
